namespace Fs {
    
    enum VolumeType {
        VolumeTypeLocalDisk,
        VolumeTypeRemoteDisk
    };

    enum FileType
    {
        FileTypeFile,
        FileTypeDirectory,
        FileTypeShortcut,
        FileTypeDisk
    };
}