namespace math {
    template <typename T>
    T max(T a, T b);

    template <typename T>
    T min(T a, T b);

    template <typename T>
    T abs(T val);

    template <typename T>
    T sqrt(T val);

    template <typename T>
    T fsqrt(T val);
}